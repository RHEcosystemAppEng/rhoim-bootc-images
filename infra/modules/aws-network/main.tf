# Common AWS networking data sources
# Use this module to get VPC and subnet information for EC2 instances

data "aws_vpc" "default" {
  default = true
}

data "aws_subnets" "default" {
  filter {
    name   = "vpc-id"
    values = [data.aws_vpc.default.id]
  }
}
