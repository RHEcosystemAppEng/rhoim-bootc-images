# RHOIM Environment Variables for bootc Model Serving

# --- Core Paths and Configuration (Used by Containerfile and Initializer) ---
MODEL_ID="TinyLlama/TinyLlama-1.1B-Chat-v1.0"
MODEL_PATH="/opt/app-root/models"
VLLM_PORT="8000"
VLLM_HOST="0.0.0.0"

# --- VLLM/Device Configuration (Critical for CPU deployment) ---
# CRITICAL FIX: Tells vLLM to use CPU path and avoids searching for CUDA libraries
VLLM_TARGET_DEVICE="cpu"

# CRITICAL FIX: Explicitly prevents vLLM from attempting to compile/import CUDA bindings.
VLLM_NO_CUDA=1 

# Optional flags to ensure CPU compatibility and debug logging
VLLM_USE_CUDA=0
VLLM_USE_FLASHINFER=0
VLLM_LOGGING_LEVEL=DEBUG

# --- Gateway/API Configuration (Used by rhoim-gateway.service) ---
API_KEYS="devkey1,devkey2"
# BASE_PATH=/ # (If used by your gateway)
# ENABLE_RHOAI_ALIAS=true # (If used by your gateway)